# 3.7 — README + Javadoc + publicação (OPCIONAL)

## Problema
Sem `README.md`; não publicado no Maven Central; Javadoc ausente na maior parte da API pública.

## Impacto
Baixa descoberta/adoção; API pública pouco documentada.

## Risco
Baixo.

## Arquivos
- novo `README.md` (raiz)
- Javadoc nos beans públicos (`SpringSessionLiteService`, `...User`, `...CookieManager`, etc.)
- `pom.xml` / `~/.m2/settings.xml` (pipeline Central) — `docs/04`, `docs/05`

## Solução / passos
1. README com: o que é, instalação, quickstart (login + `@CurrentSession` + logout), tabela de
   propriedades (link p/ docs), licença. **Refletir estado final pós-correções.**
2. Javadoc objetivo nos pontos de extensão e API pública.
3. Finalizar pipeline de release: GPG key, token Central Portal, `release` profile,
   `autoPublish`. Seguir `docs/04-publicar-no-maven-central.md`.
4. Sincronizar versão em todos os lugares (pom, docs, README).

> **Onda 4 — fechamento**, junto de 1.2.

## Complexidade
Baixa

## Verificação
- README renderiza e snippets compilam.
- `./mvnw -Prelease verify` gera sources+javadoc+assinaturas sem erro (com GPG configurado).
